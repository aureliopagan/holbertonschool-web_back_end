#!/usr/bin/env python3
"""
This module provides a function to compute the floor of a float.
"""

import math


def floor(n: float) -> int:
    """
    Returns the floor of a float.

    Args:
        n (float): The float to compute the floor of.

    Returns:
        int: The floor of the float.
    """
    return math.floor(n)
